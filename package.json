{
  "name": "expressbugtest",
  "version": "1.0.0",
  "description": "Shows some express behaviour that does not match its doc.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  }
}
